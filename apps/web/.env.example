NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1

# WebSocket de colaboración en tiempo real (Hocuspocus, embebido en el mismo backend).
# No lleva el prefijo /api/v1 porque no es una ruta REST. En producción usar wss://.
NEXT_PUBLIC_COLLAB_WS_URL=ws://localhost:4000/collaboration
